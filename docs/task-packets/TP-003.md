---
id: TP-003
status: done
lane: normal
adapter: codex
complexity: judgment
allowed_files:
  - .github/workflows/ci.yml
  - PROJECT_STATE.md
  - package.json
  - pnpm-lock.yaml
  - source.config.ts
  - styles/tokens.css
  - styles/site.css
  - scripts/check-private.sh
  - scripts/hooks/pre-push
  - scripts/validate-design.ts
  - scripts/check-js-budget.mjs
  - scripts/lighthouserc.cjs
  - app/layout.tsx
  - app/book/page.tsx
  - app/essays/page.tsx
  - components/site-header.tsx
  - components/search-dialog.tsx
  - content/essays/sample-essay.mdx
  - lib/content/constrained-mdx.ts
  - lib/content/schemas.ts
  - lib/content/validate.ts
  - docs/UPGRADE-GATES.md
  - docs/task-packets/TP-003.md
forbidden_files:
  - DEVELOPMENT-PLAN.md
  - design/DESIGN-BUILD-NOTES.md
  - AGENTS.md
  - CLAUDE.md
  - CODEX.md
  - VERIFY.md
  - ADVERSARY.md
  - WORKING-PROTOCOL.md
  - roles/
  - workflow/
verification:
  - "pnpm lint"
  - "pnpm typecheck"
  - "pnpm test"
  - "pnpm build"
  - "scripts/check-private.sh"
  - "pnpm perf:lab"
adversary: not-required
created: 2026-08-27
created_by: Codex IMPLEMENT — SITE-P10
---

# TP-003 — CI gates and hardening

## Goal

Add local private-identifier enforcement, GitHub Actions, content and design validation, performance budgets, and the schema/heading/link hardening required for the Phase 1 scaffold.

## Source of Truth

- `DEVELOPMENT-PLAN.md` — Phase 1 items 5–10.
- `design/DESIGN-BUILD-NOTES.md` — sections 15–18.
- `WORKING-PROTOCOL.md` — amended checkpoint-batch authorization.
- The repository — Rule Zero authority.

## Stage Status

| Stage | Scope | Status |
|---|---|---|
| 1 | Private scanner and pre-push hook | Complete |
| 2 | CI workflow and automated gates | Complete |
| 3 | Validation hardening | Complete |

## Hook Setup

Run `git config core.hooksPath scripts/hooks` once in every clone; this setting replaces `.git/hooks` entirely, so every future repository hook must live under `scripts/hooks/`.

The pre-push hook fails closed when `BUILDERSBOOK_PRIVATE_MANIFEST` is unset or invalid. `git push --no-verify` is an operator-only emergency override.

## Rationale

The private manifest never leaves the operator's machine. The scanner therefore runs locally at push time; CI must not expect the manifest and must not fail because it is absent.

## Assumptions

- **F-6 accepted disposition:** VERIFY report artifacts are transient by convention (chat-borne). Fixture provenance is corroborated via task-packet notes and is not independently verifiable from the repository. Accepted.
- **IndexNow deployment tradeoff:** IndexNow is dormant while production uses CLI deploys because the automation depends on the `deployment_status` trigger. It wakes when Phase 2 connects Git. Accepted.

## Upgrade Gate

See [`docs/UPGRADE-GATES.md`](../UPGRADE-GATES.md), especially the required Turbopack re-test at every `fumadocs-mdx` version bump.

## Findings

| Stage | Finding | Resolution |
|---|---|---|
| 1 | The private manifest is intentionally unavailable to CI. | The scanner is a fail-closed local pre-push hook; CI does not invoke it. |
| 1 | Git does not version `core.hooksPath`. | Every clone must run the one-line setup command above. |
| 1 | The pre-push private scan covers the current working tree, not the commit range being pushed. | This is an accepted launch tradeoff; scanning `git diff <remote-sha>..<local-sha>` remains a possible future hardening. |
| 2 | Lighthouse lab runs cannot measure field INP and hosted-runner timing is variable. | LCP and CLS are report-only; TBT is the report-only interaction proxy. The 100 KiB first-party JS budget remains a hard post-build failure. |
| 2 | The initial search implementation placed the full search client in every page's initial graph. | Search now loads on first use; the hard budget reports 36.06 KiB compressed application JS for each reading route. |
| 3 | The draft essay fixture previously carried a publication date, so it did not prove the conditional schema. | The fixture now omits `publishedAt`; schema tests require it only for published essays and reject impossible dates. |
| 3 | Dark `rule-strong` on `code-bg` was retained as a regression fixture beyond the review's named failing-pair set. | The deliberate additional fixture protects a dark code-block boundary while the two missing light-mode review pairs are now also covered. |

## Done Criteria

- [x] Only files listed in `allowed_files` were modified.
- [x] All three stages and their declared verification pass.
- [x] The local pre-push hook is enabled and fails closed.
- [x] `status: done` is set and the resulting full diff is ready for cross-family VERIFY.
